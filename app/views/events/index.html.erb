<h1>项目 | <%=link_to '动态', events_path %> | 日历 | 周报 | 团队 | 我自己 | 知人</h1>
all events 
<%= link_to 'sign out', destroy_user_session_path, method: :delete %>
<div id="events" data-last_id= "<%= @events.first&.id%>" >
  <%@events.each do |event| %>
    <%= render event %>
  <%end%>
</div>